class Rectangle:
    """TODO: implement.

    - __init__(self, width, height): store both.
    - a read-only `area` property: width * height.
    - __repr__: returns "Rectangle(width=<w>, height=<h>)".
    - __eq__: two Rectangles are equal if width and height both match.
    """
    pass
