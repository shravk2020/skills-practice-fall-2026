def unique_words(text):
    """Return the set of unique lowercase words in text (split on whitespace).

    TODO: use .split() + .lower(), build a set.
    """
    pass


def shared_tags(tags1, tags2):
    """tags1 and tags2 are lists of strings. Return the set of tags that
    appear in BOTH.

    TODO: convert to sets and use & (intersection).
    """
    pass


def only_in_first(a, b):
    """a and b are lists. Return the set of items in a that are NOT in b.

    TODO: convert to sets and use - (difference).
    """
    pass


def clean_sentence(s):
    """Strip leading/trailing whitespace, then lowercase the whole string.

    TODO: use .strip() and .lower().
    """
    pass


def split_first_rest(items):
    """items is a list with at least one element. Return a tuple
    (first, rest) where first is the first element and rest is a LIST of
    everything else.

    TODO: use unpacking with a * (e.g. `first, *rest = items`).
    """
    pass


def title_case_sentence(s):
    """Given a sentence like "the quick brown fox", return
    "The Quick Brown Fox".

    TODO: use .split(), .title() (or .capitalize() per word), and .join().
    Hint: str.title() applied to the whole sentence at once behaves oddly
    with certain inputs -- splitting into words and joining is more
    predictable and is what's being asked for here.
    """
    pass
