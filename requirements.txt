# One venv for the whole lab. Set up once:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
#
# Some packages are only needed in later weeks (torch, pymongo, sqlmodel) --
# installing everything up front means you never have to stop and pip install
# mid-lesson.

# Testing & type-checking (Days 1-40)
pytest==8.3.3
pytest-cov==5.0.0
mypy==1.11.2
httpx==0.27.2
respx==0.21.1

# FastAPI & friends (Days 11-15, 20, 24, 33)
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# ML (Days 16-20)
numpy==2.1.1
scikit-learn==1.5.2
torch==2.4.1

# Databases (Days 21-25)
pymongo==4.9.1
sqlmodel==0.0.22
